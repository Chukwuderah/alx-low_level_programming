#include "main.h"

/**
 * _islower - a function that checks for lowercase characters
 * @c: character to be checked
 * Return: 1 if c is in lowercase, otherwise 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
